{
  "name": "ui-html-canvas",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build:client": "bun build src/client/main.ts --outfile dist/public/main.js",
    "build:server": "bun build src/server/index.ts --target bun --outfile dist/server.js",
    "build": "rm -rf dist && mkdir -p dist/public && cp public/index.html public/styles.css dist/public/ && bun run build:client && bun run build:server",
    "start": "bun run build && bun run dist/server.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
